<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.kotlin.sportpro">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:configChanges="orientation"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/SportPro"
        android:usesCleartextTraffic="true"
        tools:targetApi="m">
        <activity android:name=".FilterActivity"/>
        <activity android:name=".ui.home.sport.SportActivity" />
        <activity android:name=".ui.profile.coach.sportsman.AddSportsmanActivity" />
        <activity android:name=".ui.home.AllSportActivity" />
        <activity android:name=".ui.profile.coach.event.SubmitApplicationActivity" />
        <activity android:name=".ui.profile.ProfileInfoActivity" />
        <activity android:name=".ui.event.about.AboutEventActivity" />
        <activity android:name=".ui.news.about.AboutNewsActivity" />
        <activity android:name=".HomeActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>